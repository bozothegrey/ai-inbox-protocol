# AGENTS.md

This is the operating protocol for Stefano's AI Inbox workflow.

Any AI system, automation, script, or agent that creates or modifies notes in the Notion `AI Inbox` should read this file before writing.

## System model

The system separates raw material, AI processing, long-term knowledge work, and operating rules.

| Layer | Location | Purpose |
|---|---|---|
| Raw/source archive | Google Drive | PDFs, screenshots, article exports, transcripts, source files, raw captures |
| AI working layer | Notion `AI Inbox` | AI notes, mobile captures, source summaries, working notes, review queue, promotion candidates |
| Curated knowledge work | Personal Wiki | Reviewed, structured, cumulative wiki notes and research outputs |
| Protocol and templates | GitHub `ai-inbox-protocol` | Rules, templates, workflow docs, agent instructions |

## Core principle

Notion is the cockpit and working layer. It is not the final Personal Wiki.

Raw/source files stay in Google Drive by default.

Reviewed permanent knowledge work eventually moves to the Personal Wiki when it is ready for structured synthesis.

GitHub is the canonical source of workflow rules.

## Relationship with Personal Wiki

The Personal Wiki is not a simple article-summary store.

It is a structured AI-assisted knowledge-production system that accumulates multiple sources into durable notes, frameworks, and synthesis pages.

The AI Inbox should feed the Personal Wiki only when material is ready to become part of a cumulative synthesis workflow.

A Notion page is not automatically a Personal Wiki note.

Use the AI Inbox for capture, triage, source staging, source summaries, synthesis sketches, link suggestions, and promotion candidates.

Use the Personal Wiki for durable structured pages, multi-source synthesis, source-backed analytical updates, framework development, and project-specific refresh workflows.

## Personal Wiki project registry

Before suggesting Personal Wiki promotion, consult:

```text
personal-wiki/PROJECT_REGISTRY.md
```

Current registered Personal Wiki projects are:

- EM Macro
- AI Economy
- Energy
- Geopolitics
- Personal Trainer

The following domains are excluded from Personal Wiki promotion by default:

- BoGa app/product work
- Music practice and theory
- Books and reading impressions
- Family logistics
- Random tech fixes

These can still live in AI Inbox as notes, captures, or working material. They should not be routed to the Personal Wiki unless the registry is explicitly updated.

## Notion database

Main database:

```text
AI Inbox
```

Expected properties:

| Property | Type | Purpose |
|---|---|---|
| `Name` | Title | Note title |
| `Author` | Select | Who created or imported the note |
| `Type` | Select | Kind of note |
| `Status` | Status | Workflow stage |
| `Review` | Select | Review state |
| `Domain` | Multi-select | Topic or project domain |
| `Source` | URL | Original source URL, if any |
| `Raw Location` | URL | Google Drive raw/source link, if any |
| `Target` | Select | Intended destination or archive target |
| `Created` | Created time | Automatic creation timestamp |
| `Last Edited` | Last edited time | Automatic edit timestamp |

## Author policy

Set `Author` accurately.

Use:

- `Sergio` for notes generated or substantially edited by Sergio.
- `Stefano` for notes written directly by Stefano.
- `Other AI` for notes produced by another AI assistant or tool.
- `Imported` for material imported from another system without substantial editing.

When Sergio creates a page, set:

```text
Author = Sergio
Review = Pending
```

## Type policy

Use the most accurate `Type`.

Recommended values:

- `AI Note` — structured note generated by Sergio or another AI.
- `Mobile Capture` — quick mobile capture, OCR, link, screenshot summary, or rough idea.
- `Source Summary` — summary of an external source, document, article, email, or raw material.
- `Working Note` — synthesis, map, analysis, intermediate thinking, link suggestions.
- `Promotion Candidate` — cleaned material ready to enter a Personal Wiki workflow after review.

## Status and review policy

Use `Status` for workflow stage and `Review` for human review state.

Current Notion status groups may use standard labels such as:

- `Not started`
- `In progress`
- `Done`

Use `Review` to track review state:

- `Pending`
- `Reviewed`
- `Rejected`

Do not mark material as ready for the Personal Wiki unless it is a reviewed `Promotion Candidate` and it fits the Personal Wiki project registry.

## Domain policy

Use `Domain` to make the note easy to retrieve and summarize.

Recommended values:

- `BoGa`
- `Fitness`
- `Music`
- `Sovereign Debt`
- `Tech`
- `Reading`
- `Family`

Use multiple domains when appropriate.

AI Inbox domains are not the same as Personal Wiki project admission. A note may be useful in AI Inbox while still being outside Personal Wiki scope.

## Source and raw-location policy

Raw/source material lives in Google Drive by default.

When a source exists, set:

```text
Source = original URL or reference
Raw Location = Google Drive raw/source link, if known
```

Notion should store processed notes, summaries, OCR text, comments, maps, drafts, indexes, and review notes.

## Standard page structure

New structured notes should normally use this body structure:

```markdown
## Summary

## Main Note

## Links / Suggested Links

## Source / Raw Location

## Review / Next Actions
```

For source summaries, prefer:

```markdown
## Source

## Why It Matters

## Summary

## Key Points

## Concepts / Entities

## Suggested Links

## Next Actions
```

For promotion candidates, prefer:

```markdown
## Summary

## Final Note / Synthesis Brief

## Suggested Personal Wiki Workflow

## Source / Raw Location

## Review
```

## Promotion policy

The Personal Wiki is the curated destination for structured knowledge work.

Notion is not the canonical archive.

A note should become a `Promotion Candidate` before it is routed into a Personal Wiki workflow.

After promotion, update the Notion page accordingly and avoid maintaining two competing canonical versions.

## Minimal fallback

If this protocol cannot be fully consulted, create only a minimal capture note and mark it for review.

Minimum fields:

```text
Name = clear title
Author = accurate author
Type = Mobile Capture or AI Note
Review = Pending
```

Minimum body:

```markdown
## Summary

## Captured Content

## Source / Raw Location

## Review / Next Actions
```

## Notion read-first page

The Notion `AI Inbox` should contain a minimal page named:

```text
AGENTS.md
```

That page should point here rather than duplicating this protocol.

Canonical repository:

https://github.com/bozothegrey/ai-inbox-protocol

Canonical instructions:

https://github.com/bozothegrey/ai-inbox-protocol/blob/main/AGENTS.md
